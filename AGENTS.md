# 시험감독 시간표 작업 지침

## 목적과 범위

교사가 이름을 입력하면 전국연합학력평가의 시험감독 배정을 조회하는 정적 웹앱이다. 이 저장소에서는 원본 배정표를 바꾸지 않고, 검증된 원본에서 웹앱 데이터를 재생성한다.

## 데이터 원칙

- 현재 웹앱의 기준 원본은 저장소 최상위의 9월 2일 전국연합학력평가 PDF이다.
- `schedule_data.js`는 기준 PDF에서 `parse_supervision_pdf.py`로 생성되는 파생 파일이다. 원본을 수정한 뒤에만 재생성한다.
- `.hwp`, `.hwpx`, `.xlsx`는 원본 증빙 파일이다. 요청이 명확하지 않으면 이름 변경·덮어쓰기·삭제하지 않는다.
- `extracted_tables.json`, `extract.py`, `process_data.py`, `parse_md.py`, `parse_june4.py`, `verify_data.py`는 이전 변환 경로의 보관물이다. 현재 9월 2일 화면을 갱신할 때에는 사용하지 않는다.
- `thursday_data.js`와 `parse_thursday.py`는 별도 목요일 시간표 데이터이며, 현재 화면의 `scheduleData`와 혼합하지 않는다.

## 작업 절차

1. 변경 전 `git status --short`로 사용자의 미추적 파일과 변경 사항을 확인한다.
2. 원본 배정표의 교사·시간·반·시험명 변경은 먼저 PDF 기준 원본에 반영한다. HWP/HWPX 원본을 직접 수정해야 하면 별도 요청과 검토를 받는다.
3. `make build`로 `schedule_data.js`를 재생성하고, `make check`를 통과시킨다.
4. 화면 변경 후 `make serve`로 브라우저에서 검색 결과를 확인한다. 데이터만 바뀐 경우에도 교사 한 명 이상을 실제로 검색해 확인한다.
5. 데이터 수 또는 배정이 달라졌다면 변경 이유, 교사 수, 총 배정 수, 충돌 여부를 함께 보고한다.

## 검증 기준

- 모든 배정에는 `grade`, `class`, `exam`, `time`, `period`가 있어야 한다.
- 시간은 `HH:MM~HH:MM` 형식이고 종료 시각이 시작 시각보다 늦어야 한다.
- 교사별 감독 시간이 서로 겹치면 실패로 처리한다.
- `7교시(62분) 담임`, `8교시(33분) 부담임`, `부담임(33분)`은 유효한 감독 구분이다.

## 안전 경계

- 외부 배포, 원격 저장소 푸시, HWP/HWPX/XLSX 원본 변경은 사용자의 명시적 요청이 있을 때만 수행한다.
- 변경 범위가 불명확하면 파생 파일을 임의로 재생성하거나 최신 원본을 추정하지 말고, 기준 Markdown 파일을 확인한다.
